[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "physical-ai-data-factory-sim"
version = "0.1.0"
description = "Deterministic physical-AI validation scenario generator"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["physical_ai_data_factory*"]
